<tr>
  <td class='image_column'>
    <%= link_to image_tag('place.png', alt: 'Place'), place_path(place) %>
  </td>
  <td>
      <%= link_to place.name, place_path(place) %>
      <span class="books_count">
        Books count:
        <strong>
          <%= place.books.count %>
        </strong>
      </span>
  </td>
</tr>
