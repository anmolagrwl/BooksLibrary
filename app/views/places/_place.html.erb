<li>
  <div>
    <%= link_to image_tag('place.png', alt: 'Place'), place_path(place), class: 'book_image' %>
    <div class="place_details">
      <%= link_to place.name, place_path(place) %>
      <span class="books_count">
        Books count:
        <strong>
          <%= place.books.count %>
        </strong>
      </span>
    </div>
  </div>
</li>